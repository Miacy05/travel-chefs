# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# 日志
*.log
npm-debug.log*

# 依赖（测试用 jsdom 才会产生；部署不需要）
node_modules/
package-lock.json

# 旧版原型备份（保留在本地，不进仓库，避免部署时混淆 index.html）
backup-v1/

.vercel
